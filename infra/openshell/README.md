# OpenShell Infrastructure

NVIDIA OpenShell configuration for sandbox environments.

## Contents (Phase 4)

- Dockerfile for sandbox images (Node.js + pi-coding-agent + gRPC server)
- OpenShell policy YAML templates
- Credential provider configuration
